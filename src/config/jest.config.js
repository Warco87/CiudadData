module.exports = {
  preset: "ts-jest",              // usa ts-jest para compilar TS
  testEnvironment: "node",        // entorno Node.js
  //falta las carpetas, estas se ejecutaran con el comando npm run test y sera con cobertura
  verbose: true
};